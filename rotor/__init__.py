"""Package identity for Rotor, a small Sera market maker."""

# Keep the version in one importable place so the CLI can report it without
# importing the runtime modules that may touch network/wallet dependencies.
__version__ = '0.2.0'
