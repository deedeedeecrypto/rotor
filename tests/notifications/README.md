# Notification Tests

This folder tests optional outbound notification behavior.

Files:

- `test_telegram.py`: Telegram channel routing, dry-run behavior, error
  swallowing, heartbeat rate limiting, and optional dependency fallback.
